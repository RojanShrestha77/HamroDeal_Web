// Custom commands for Cypress
// You can add custom commands here if needed

declare global {
    namespace Cypress {
        interface Chainable {
            // Add custom commands here
        }
    }
}

export { };
